# Optional: full backend API URL when your FastAPI backend is hosted separately.
# Leave unset for local Vite proxy (/api -> http://127.0.0.1:8000).
VITE_API_BASE_URL=https://your-backend-domain.example.com/api
